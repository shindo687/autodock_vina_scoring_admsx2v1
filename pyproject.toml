[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "vina-ad"
version = "0.2.0"
description = "ChainRules-compatible differentiable coordinate scoring sidecar for AutoDock Vina"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
license = {text = "Apache-2.0"}
authors = [{name = "AutoDock-Vina AD contributors"}]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
